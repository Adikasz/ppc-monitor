# Discord bot
discord.py==2.4.0
# audioop-lts backport: Python 3.13 eltávolította a stdlib `audioop` modult,
# de a discord.py 2.4 még importálja (hangkezeléshez). A bot szöveges, de a
# csomag importja a teljes stack betöltését igényli.
audioop-lts==0.2.2; python_version >= "3.13"

# Supabase
# 2.17.0 supports the new sb_publishable_/sb_secret_ key format AND avoids
# the pyiceberg dependency that storage3>=2.x pulls in (pyiceberg needs MSVC
# to build on Windows + Python 3.14). Bump when a Python-3.14 pyiceberg wheel
# ships, or when storage3 drops the pyiceberg requirement.
supabase==2.17.0

# Meta Ads API
facebook-business==21.0.0

# Google Ads API
# Az integráció kódja (src/integrations/google_ads.py) KÉSZ, de az SDK importja
# lazy (csak a GoogleAdsClient.__init__-ben) — így a bot/discovery telepített SDK
# nélkül is elindul, a Google hívás kontrollált RuntimeError-t ad token/SDK híján.
# A csomag itt KIKOMMENTELVE marad, mert google-ads még nem szállít Python 3.14
# wheelt (`pip install` elhasalna). Aktiváld (google-ads>=31.0.0), amint van
# 3.14-kompatibilis wheel VAGY Python 3.12/3.13 alatt futtatod a discovery-t.
# google-ads==31.0.0

# Claude (Anthropic)
anthropic==0.39.0

# ClickUp — egyszerű HTTP, nincs dedikált SDK, requests-szel kezeljük
requests==2.32.3

# Környezeti változók
python-dotenv==1.0.1

# Időkezelés (timezone)
pytz==2024.2

# Ütemezés (cron-szerű, álló bot mellé)
apscheduler==3.10.4

# Fejlesztés / tesztelés
pytest==8.3.3
pytest-asyncio==0.24.0
